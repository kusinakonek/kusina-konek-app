/**
 * FoodItem
 * Shape for donation/rescue listings.
 *
 * @typedef {Object} FoodItem
 * @property {string} id
 * @property {string} title
 * @property {string} description
 * @property {{ latitude: number, longitude: number, address?: string }} location
 * @property {string} donorId
 * @property {'available' | 'claimed'} status
 */

export {}; // keeps this file as a module
