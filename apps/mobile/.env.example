# Copy to `.env` and adjust for your machine/network.

# Backend port (must match apps/server env PORT)
EXPO_PUBLIC_API_PORT=3000

# Optional: set a fixed host/IP (otherwise auto-detected from Expo dev server)
# EXPO_PUBLIC_API_HOST=192.168.1.12
